import { CategorieBbPipe } from './categorie-bb.pipe';

describe('CategorieBbPipe', () => {
  it('create an instance', () => {
    const pipe = new CategorieBbPipe();
    expect(pipe).toBeTruthy();
  });
});
