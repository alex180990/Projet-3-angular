import { CategorieCcPipe } from './categorie-cc.pipe';

describe('CategorieCcPipe', () => {
  it('create an instance', () => {
    const pipe = new CategorieCcPipe();
    expect(pipe).toBeTruthy();
  });
});
