import { FormatNombrePipe } from './format-nombre.pipe';

describe('FormatNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
